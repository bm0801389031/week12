//function statement named seatsInTheater with four parameters
// nCols, nRows, col, row.
function seatsInTheater(nCols, nRows, col, row) {
    //return nCols minus col plus 1 times nRows minus rows
    //follow the order of operations
        return (nCols-col+1) * (nRows - row);
    }