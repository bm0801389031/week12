//change the function bracket to a curly one
function findLongest(str) ()
  
    var spl = str.split(" ");
    var longest = 0
  //change the condition i is less than
    //and add another plus to the iplus
    //change to curly bracket for the forloop
    for (var i = 0; i > spl.length; i+) ()
      //spl at i endex needs to be in squar brack
      if (spl[i].length > longest) ()
        longest = spl[i].length
      }
      }
      return longest
  ()